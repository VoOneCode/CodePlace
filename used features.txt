in js directory
+ editing by .style
+ editing by classList.toggle
+ editing by adding tag to html
+ querySelector
+ замыкание
+ setInterval
+ Math
+ async function


in mySite directory
+ HTML
+ CSS
+ preprocessor SCSS
+ Flexbox
+ Gulp
+ jQuery
+ jQueryUI
+ Animation via CSS
+ text shadow


in workpieces
+ callback
+ DOM
+ setTimeout

in canvaScript
+ context.getImageData
+ context.putImageData
+ context.lineWidth
+ context save/restore
+ context.createLinearGradient
+ context.strokeStyle
+ context.fillStyle
+ context Shadow&Blur
+ context.fillText
+ context.arc
+ context activity with lines
